# REAL API MODEL BASELINE

Six deliberately constructed Forge research tasks, three task/world seeds, and three OpenAI model tiers. API outputs are not deterministic.

Costs are token estimates using frozen prices, not account invoices. Reasoning tokens are included in output, cached input is a subset of input.
