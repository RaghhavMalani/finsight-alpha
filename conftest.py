"""pytest configuration.

The mere presence of this file at the project root makes pytest insert the
project root onto ``sys.path``, so tests can ``import src.*`` without any extra
installation step.
"""
