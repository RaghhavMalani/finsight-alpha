# FinSight Alpha - local-first dependencies
# Install with:  pip install -r requirements.txt
#
# This is everything the local Streamlit dashboard needs. No cloud accounts,
# API keys, or databases required.

# --- Core data & analytics ---
pandas
numpy
yfinance
scipy

# --- Visualization & dashboard ---
plotly
streamlit

# --- ML Forecasting ---
scikit-learn
joblib
hmmlearn

# --- Local storage (Parquet engine) ---
pyarrow

# --- RAG Financial Document Intelligence ---
pypdf
python-docx
sentence-transformers
faiss-cpu
chromadb
rank-bm25
tqdm
requests
beautifulsoup4
lxml
urllib3
tldextract
# googlesearch-python
# duckduckgo-search
# Optional local/cloud LLMs (the RAG answer engine works without any of these
# via local Ollama over HTTP; install only the one you want to use):
# ollama                 # local models, free  (run `ollama serve`, then `ollama pull llama3.1`)
# openai                 # set OPENAI_API_KEY
# anthropic              # set ANTHROPIC_API_KEY
# google-generativeai    # set GOOGLE_API_KEY

# --- Config & environment ---
python-dotenv

# --- Testing ---
pytest

# ---------------------------------------------------------------------------
# OPTIONAL / PAUSED: cloud + API stack (Phase 1C/1D, not required locally).
# Cloud deployment is paused to avoid cost - the dashboard runs fully without
# these. Uncomment only if you revisit the FastAPI backend or cloud uploads.
# ---------------------------------------------------------------------------
# fastapi
# uvicorn
# requests
# pydantic
# sqlalchemy
# psycopg2-binary
# google-cloud-bigquery
# google-cloud-storage
