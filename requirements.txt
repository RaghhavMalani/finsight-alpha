# FinSight Alpha - local-first dependencies
# Install with:  pip install -r requirements.txt
#
# Everything the API backend + analytics engine needs. No cloud accounts,
# API keys, or databases required.

# --- Core data & analytics ---
pandas
numpy
yfinance
scipy

# --- Visualization (Plotly charts) ---
plotly

# --- ML Forecasting ---
scikit-learn
joblib
hmmlearn

# --- Local storage (Parquet engine) ---
pyarrow

# --- RAG Financial Document Intelligence ---
pypdf
python-docx
# sentence-transformers  -> moved to requirements-rag.txt (too heavy for Vercel)
# faiss-cpu              -> moved to requirements-rag.txt
# chromadb               -> moved to requirements-rag.txt
rank-bm25
tqdm
requests
beautifulsoup4
lxml
urllib3
tldextract
# googlesearch-python
# duckduckgo-search
# Optional local/cloud LLMs (the RAG answer engine works without any of these
# via local Ollama over HTTP; install only the one you want to use):
# ollama                 # local models, free  (run `ollama serve`, then `ollama pull llama3.1`)
openai                   # set OPENAI_API_KEY, or used for Azure OpenAI (gpt-5-mini)
# anthropic              # set ANTHROPIC_API_KEY
# google-generativeai    # set GOOGLE_API_KEY

# --- Agentic AI: Microsoft Agent Framework (MAF) ---
# Powers the AI AGENT tab with native tool-calling on Azure gpt-5-mini.
# Install with:  pip install agent-framework
# The /agent endpoint falls back to a built-in loop if this isn't installed.
agent-framework

# --- Config & environment ---
python-dotenv

# --- Testing ---
pytest

# ---------------------------------------------------------------------------
# Server / API stack (FastAPI backend + auth + Postgres). Required to run the
# terminal backend and to build the deployment container.
# ---------------------------------------------------------------------------
fastapi
uvicorn[standard]
pydantic
sqlalchemy
psycopg2-binary          # PostgreSQL driver (Azure Database for PostgreSQL)
sentry-sdk[fastapi]      # optional at runtime; enabled when SENTRY_DSN is set

# Optional cloud upload stack (not needed for the terminal; uncomment if used):
# google-cloud-bigquery
# google-cloud-storage
