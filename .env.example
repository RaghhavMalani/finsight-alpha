# FinSight Alpha - example environment file.
# Copy this file to ".env" and fill in real values. The ".env" file is git-ignored.
#
#   cp .env.example .env        # macOS / Linux
#   copy .env.example .env      # Windows
#
# Values are loaded via python-dotenv (see src/config.py). Everything here is
# OPTIONAL for local development - the app runs without any of it set.

# --- API / dashboard wiring -------------------------------------------------
# Base URL the Streamlit dashboard uses when running in "API Mode".
API_BASE_URL=http://127.0.0.1:8000

# Default market-data provider used by the service when none is specified.
MARKET_DATA_PROVIDER=yfinance

# --- Google Cloud: project & deployment ------------------------------------
# These identify the GCP project and the region everything is deployed to.
GCP_PROJECT_ID=finsight-alpha-498208
PROJECT_NUMBER=771358783484
REGION=asia-south1

# --- Google Cloud: BigQuery (analytics warehouse) -------------------------
BIGQUERY_DATASET=finsight_alpha
BIGQUERY_MARKET_PRICES_TABLE=market_prices_daily
BIGQUERY_ANALYTICS_TABLE=market_analytics_daily

# --- Google Cloud: Cloud Storage (raw files / PDFs) -----------------------
GCS_BUCKET_NAME=finsight-alpha-498208-finsight-alpha-data

# Path to a service-account JSON key file for BigQuery / Cloud Storage auth.
# Leave empty when running on Cloud Run (it uses the attached service account).
GOOGLE_APPLICATION_CREDENTIALS=

# --- Database (Cloud SQL / PostgreSQL for app metadata, optional) ----------
# SQLAlchemy URL. Example for local Postgres:
DATABASE_URL=postgresql://username:password@localhost:5432/finsight_alpha

# --- Market data provider API keys (optional) ------------------------------
ALPHA_VANTAGE_API_KEY=
POLYGON_API_KEY=
# Finnhub: free real-time/intraday quotes. Get a key at finnhub.io/dashboard.
# Put the REAL key only in your .env (git-ignored), never in committed code.
FINNHUB_API_KEY=your-finnhub-key-here

# --- LLM providers (use ANY one; powers RAG, the graph, and the AI Agent) ---
# Azure OpenAI (recommended for a hosted/cloud LLM). A key alone is NOT enough -
# you also need the endpoint and your deployment name from the Azure portal.
AZURE_OPENAI_API_KEY=your-regenerated-key-here
AZURE_OPENAI_ENDPOINT=https://your-resource-name.openai.azure.com
AZURE_OPENAI_DEPLOYMENT=your-deployment-name        # e.g. gpt-4o-mini
AZURE_OPENAI_API_VERSION=2024-06-01

# Other options (pick one). Local Ollama needs no key.
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
# GOOGLE_API_KEY=
# GROQ_API_KEY=

# Be a good SEC citizen for EDGAR fair-access (optional):
# FINSIGHT_SEC_USER_AGENT=FinSight Alpha (your-email@example.com)
